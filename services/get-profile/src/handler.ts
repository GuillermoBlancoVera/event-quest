import { DynamoDBClient } from '@aws-sdk/client-dynamodb'; import { DynamoDBDocumentClient, BatchGetCommand, GetCommand } from '@aws-sdk/lib-dynamodb'; import type { APIGatewayProxyHandlerV2 } from 'aws-lambda'; import type { Challenge, PlayerProfile, User } from '@event-quest/shared';
const db=DynamoDBDocumentClient.from(new DynamoDBClient({})); const json=(statusCode:number,body:unknown)=>({statusCode,headers:{'content-type':'application/json','access-control-allow-origin':'*'},body:JSON.stringify(body)});
export const handler:APIGatewayProxyHandlerV2=async event=>{const id=event.pathParameters?.id; if(!id)return json(400,{code:'VALIDATION_ERROR',message:'User id is required.'}); const result=await db.send(new GetCommand({TableName:process.env.USERS_TABLE,Key:{PK:`USER#${id}`,SK:'PROFILE'}})); if(!result.Item)return json(404,{code:'NOT_FOUND',message:'Player not found.'}); const {password:_,PK,SK,entity,...user}=result.Item as User & {password?:string}; const completed=user.completedChallenges??[], challenges=completed.length?await db.send(new BatchGetCommand({RequestItems:{[process.env.CHALLENGES_TABLE!]:{Keys:completed.map(challengeId=>({PK:`CHALLENGE#${challengeId}`,SK:'DETAIL'}))}}})):undefined; const history=(challenges?.Responses?.[process.env.CHALLENGES_TABLE!]??[] as Challenge[]).map(challenge=>({challengeId:challenge.challengeId,title:challenge.title,points:challenge.points})).sort((a,b)=>a.challengeId-b.challengeId); return json(200,{...user,history} satisfies PlayerProfile);};
