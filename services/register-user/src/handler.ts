import { DynamoDBDocumentClient, PutCommand } from '@aws-sdk/lib-dynamodb'; import { DynamoDBClient } from '@aws-sdk/client-dynamodb'; import type { APIGatewayProxyHandlerV2 } from 'aws-lambda'; import type { RegisterUserRequest, User } from '@event-quest/shared';
const db = DynamoDBDocumentClient.from(new DynamoDBClient({})); const json = (statusCode: number, body: unknown) => ({ statusCode, headers: { 'content-type': 'application/json', 'access-control-allow-origin': '*' }, body: JSON.stringify(body) });
export const handler: APIGatewayProxyHandlerV2 = async (event) => { const input = JSON.parse(event.body ?? '{}') as RegisterUserRequest; if (!input.name?.trim()) return json(400, { code: 'VALIDATION_ERROR', message: 'Name is required.' }); const now = new Date().toISOString(); const user: User = { userId: crypto.randomUUID(), name: input.name.trim(), team: input.team?.trim() || 'Guests', group: input.group?.trim() || 'General', score: 0, completedChallenges: [], createdAt: now, updatedAt: now }; await db.send(new PutCommand({ TableName: process.env.USERS_TABLE, Item: { PK: `USER#${user.userId}`, SK: 'PROFILE', entity: 'USER', ...user } })); return json(201, user); };
