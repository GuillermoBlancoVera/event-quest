import { DynamoDBClient } from '@aws-sdk/client-dynamodb'; import { DynamoDBDocumentClient, ScanCommand } from '@aws-sdk/lib-dynamodb'; import type { APIGatewayProxyHandlerV2 } from 'aws-lambda'; import type { User } from '@event-quest/shared'; const db=DynamoDBDocumentClient.from(new DynamoDBClient({})); export const handler: APIGatewayProxyHandlerV2=async()=>{const [users,challenges]=await Promise.all([db.send(new ScanCommand({TableName:process.env.USERS_TABLE})),db.send(new ScanCommand({TableName:process.env.CHALLENGES_TABLE}))]); const players=(users.Items??[] as User[]).filter(x=>x.entity==='USER'), total=(challenges.Items??[]).filter(x=>x.enabled).length, active=[...players].sort((a,b)=>b.completedChallenges.length-a.completedChallenges.length)[0], fastest=[...players].sort((a,b)=>a.updatedAt.localeCompare(b.updatedAt))[0]; const item=(u:User|undefined)=>u&&({rank:0,userId:u.userId,name:u.name,team:u.team,group:u.group,score:u.score,completed:u.completedChallenges.length,lastActivityAt:u.updatedAt}); return {statusCode:200,headers:{'content-type':'application/json','access-control-allow-origin':'*'},body:JSON.stringify({players:players.length,challenges:total,completionPercentage:players.length&&total?Math.round(players.reduce((n,u)=>n+u.completedChallenges.length,0)/(players.length*total)*100):0,mostActivePlayer:item(active),fastestPlayer:item(fastest)})};};
