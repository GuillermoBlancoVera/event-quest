import { Link, Outlet } from 'react-router-dom'; import { useEffect, useState } from 'react';
const links=[['/#el-dia','El gran día'],['/#finca','La finca'],['/#detalles','Detalles']]; export function Layout(){const [open,setOpen]=useState(false);const [dark,setDark]=useState(()=>localStorage.getItem('theme')==='dark'); useEffect(()=>{document.documentElement.dataset.theme=dark?'dark':'light';localStorage.setItem('theme',dark?'dark':'light')},[dark]); return <><div className="garland" aria-hidden="true"><span>✦</span><span>✦</span><span>✦</span><span>✦</span><span>✦</span></div><header className="site-header"><Link className="brand" to="/">La Boda de <i>Lucía &amp; Mateo</i></Link><button className="menu" aria-expanded={open} aria-label="Abrir navegación" onClick={()=>setOpen(!open)}>☰</button><nav className={open?'open':''}>{links.map(([to,label])=><a key={to} href={to} onClick={()=>setOpen(false)}>{label}</a>)}<Link className="game-link" to="/juego" onClick={()=>setOpen(false)}>Jugar</Link><button className="theme" onClick={()=>setDark(!dark)} aria-label="Cambiar tema de color">{dark?'☼':'◐'}</button></nav></header><main><Outlet/></main><footer><span>Hecho con cariño para un día inolvidable.</span><a href="/#detalles">¿Necesitas ayuda?</a></footer></>}
