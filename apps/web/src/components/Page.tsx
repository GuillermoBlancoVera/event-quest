import type { ReactNode } from 'react'; export function Page({eyebrow,title,top,children}:{eyebrow?:string;title?:string;top?:ReactNode;children:ReactNode}){return <section className="page">{top}<p className="eyebrow">{eyebrow??'Celebremos juntos'}</p>{title && <h1>{title}</h1>}{children}</section>}
