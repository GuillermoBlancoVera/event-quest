import type { ReactNode } from 'react'; export function Page({eyebrow,title,top,children}:{eyebrow?:string;title?:string;top?:ReactNode;children:ReactNode}){return <section className="page">{top}{(title ?? eyebrow) && <h1>{title ?? eyebrow}</h1>}{children}</section>}
