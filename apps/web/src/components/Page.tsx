import type { ReactNode } from 'react'; export function Page({eyebrow,title,children}:{eyebrow?:string;title?:string;children:ReactNode}){return <section className="page"><p className="eyebrow">{eyebrow??'Celebremos juntos'}</p>{title && <h1>{title}</h1>}{children}</section>}
