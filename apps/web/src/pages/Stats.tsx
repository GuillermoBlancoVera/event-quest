import { useEffect,useState } from 'react'; import type { Stats as StatsType } from '@event-quest/shared'; import { api } from '../lib/api'; import { Page } from '../components/Page'; export function Stats(){const [stats,setStats]=useState<StatsType>();useEffect(()=>{api.getStats().then(setStats).catch(console.error)},[]);return <Page eyebrow="Los números del día" title="Momentos que florecen">{!stats?<p>Contando la magia…</p>:<div className="stat-grid"><article><b>{stats.players}</b><span>invitados</span></article><article><b>{stats.challenges}</b><span>retos</span></article><article><b>{stats.completionPercentage}%</b><span>completado</span></article><article><b>{stats.mostActivePlayer?.name??'—'}</b><span>más participativo</span></article></div>}</Page>}
